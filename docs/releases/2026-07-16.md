# 2026-07-16

Release version: `2026.7.16`

## Changes

- Adds the `docs.cli` audit scope for command-manifest, design-doc, and parser
  inventory governance.
- Adds reusable JSON Schema validation helpers for schema-backed contracts.
- Delegates the `tests` audit scope to Rack native audit from
  `wn-rack>=2026.7.16`.
- Keeps dev-std-owned test-suite policy for explicit configured test roots,
  mapped Rack audit failures, fail-closed Rack upgrade guidance, and the
  `signoff` concern on configured signoff strata.
- Documents the Rack/dev-std ownership boundary and leaves Rack strict
  `test_cases` metadata as a future migration decision.

## Validation

- Rack `2026.7.16` was published through trusted publishing before dev-std
  raised its dependency floor.
- Focused Rack-delegation tests, L0 foundation tests, L99 signoff tests,
  `dev-std audit .`, and plan hygiene passed locally during release
  preparation.
