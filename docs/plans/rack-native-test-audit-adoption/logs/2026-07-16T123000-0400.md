+++
type = "plan_log"
id = "rack-native-test-audit-adoption-2026-07-16-008"
plan_id = "rack-native-test-audit-adoption"
step_id = "release-rack-capability"
created = "2026-07-16T12:30:00-04:00"
+++

# Log: rack-current-main-rebase

Rebased the Rack audit release candidate onto the current Rack `origin/main`
after finding the local Rack checkout was stale.

## Rebase Result

The current Rack remote already used Wavenumber date-versioning and contained
new progress, release, and self-hosted test governance work. The Rack audit
work was rebased on top of that source instead of preserving obsolete local
release assumptions.

## Current Rack Candidate

- Rack branch: `main`
- Base: `origin/main` at `b1b635f`
- Candidate tip: `74f111e chore: prepare Rack 2026.7.16 release`
- Local commits ahead of `origin/main`: 5

## Adjustments After Rebase

- Preserved Rack `2026.7.16` date versioning already present on current main.
- Kept native `rack audit` and the `rack.audit_report` / `a0` JSON contract.
- Kept hardening for invalid `rack.toml` with a stratum argument and duplicate
  `[strata].order` entries.
- Added the current release notes and changelog entry for `2026.7.16`.
- Extracted parser construction to `rack.parser.build_parser()` so Rack can
  dogfood dev-std CLI inventory without increasing the legacy `cli.py` size.
- Updated Rack `dev-std.toml` to use `rack.parser:build_parser` as the
  deterministic parser inventory provider.
- Added Rack L99 `test_cases` metadata so strict `rack audit` passes.

## Verification

- `uv run python -m pytest -q tests\L0_public_cli\test_L0_003_audit.py`
- `uv run rack audit --strict --format json`
- `uv run rack run --all`
- `uv lock --check`
- `git diff --check`
- `uv run python -m build`
- `uv run twine check dist\*`
- local-source dev-std dogfood:
  `$env:PYTHONPATH=(<dev-std-src> + ';' + <wn-rack-src>); uv run --project <dev-std-checkout> dev-std audit . --scope docs.cli`

## Current Boundary

Rack `2026.7.16` is locally signed off at `74f111e`, but it has not been
published. This plan remains on `release-rack-capability` until the rebased
candidate receives external review and Rack is published through the normal
release path. dev-std should not move to the Rack dependency/delegation step
until that published Rack version exists.
