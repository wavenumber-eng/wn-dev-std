+++
type = "plan_log"
id = "rack-native-test-audit-adoption-2026-07-16-006"
plan_id = "rack-native-test-audit-adoption"
step_id = "release-rack-capability"
created = "2026-07-16T12:05:00-04:00"
+++

# Log: rack-release-review-fixes

Addressed external review findings that block Rack publication.

## Review Findings Addressed

- `rack audit L0 --format json` could crash before the audit contract handled
  invalid `rack.toml` because legacy pre-dispatch stratum normalization called
  the old config loader.
- Rack native audit did not reject duplicate `[strata].order` entries, which
  would weaken dev-std delegation compared with the current local test audit.
- Rack release versioning should follow Wavenumber date versions.

## Rack Commit

- `056e38c fix: harden Rack audit release checks`

## Changes

- Skipped legacy pre-dispatch stratum normalization for `rack audit` so audit
  failures stay inside the versioned report contract.
- Added `duplicate_stratum` failures for duplicate `[strata].order` entries.
- Added regressions for invalid `rack.toml` with a stratum argument and
  duplicate strata order.
- Updated Rack's release target from `1.2.0` to `2026.7.16`.

## Verification

- `uv run python -m pytest -q`
- `uv run rack audit --strict --format json`
- `uv run rack run --all`
- `$env:PYTHONPATH=(Resolve-Path src).Path; uv run --project C:\eli\wn-hw\wn-dev-std dev-std audit . --scope docs.cli`
- `uv run python -m compileall -q src tests`
- `uv lock --check`
- `uv run --with build python -m build`
- `uv run --with twine twine check dist\*`

## Current Boundary

Rack remains at the publication/external-review boundary. The current local
release candidate is Rack `2026.7.16` at commit `056e38c`.
